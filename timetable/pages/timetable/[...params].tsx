import { useRouter } from 'next/router'

const Timetable = () => {
    const router = useRouter()
    console.log(router);

    return (
        <div>
            
        </div>
    )
}

export default Timetable
