export type LinkProps = {
    text: string,
    href: string,
}
export type InputProps = {
    type: string,

    value?: string,
    placeholder?: string,
    onChange?: any,
    readonly?: boolean,
}